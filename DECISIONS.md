# 지금까지의 판단과 근거

`CLAUDE.md` 는 코드가 지금 어떤 상태인지를 담는다. 이 문서는 **왜 그 상태가
되었는지**를 담는다.

## 이 문서를 읽는 법

여기 적힌 것은 **규칙이 아니라 기록이다.**

전부 특정 시점에, 특정 이유로 내린 판단이다. 그 이유가 지금도 유효한지는
다시 판단해야 한다. 더 나은 근거가 있으면 뒤집어라. 이 문서의 목적은
결정을 고정하는 게 아니라, **이미 시도해 본 것과 그때 무엇이 문제였는지를
알려서 같은 벽에 두 번 부딪히지 않게** 하는 것이다.

다만 두 가지는 성격이 다르다.

- **기획이 정한 제약**(1절)은 사용자의 결정이다. 바꾸자고 제안하는 건 자유지만
  말없이 바꾸면 배포가 깨지거나 기획 의도가 사라진다. 상의 후에 바꿔라.
- **함정**은 이 문서가 아니라 `CLAUDE.md` 에 있다. 그건 취향이 아니라 사실이다.

---

## 1. 기획이 정한 제약

`PLAN.md` 에서 온 것들이다. 취향이 아니라 사용자가 정한 조건이다.

| 제약 | 왜 |
|---|---|
| 빌드 도구 · 프레임워크 없음 | 학습 목적. `index.html` 을 열면 그대로 도는 상태를 유지하려 함 |
| 서버 없음, `localStorage` 만 | 개인 제작물이고 수익화가 없다. 백엔드를 두면 유지 비용이 생긴다 |
| 진입점 `index.html`, 정적 호스팅 | GitHub Pages 로 배포한다. 빌드 단계가 생기면 배포 경로가 깨진다 |
| 결제는 전부 모의 | 실제 결제 연동은 명시적으로 범위 밖이다. 카드 정보를 입력받지 않는다 |
| 기준 캔버스 1080 × 1920 | 실제 키오스크 비율. 이 좌표계 위에 스케일링이 서 있다 |

이걸 바꾸면 좋은 이유가 생길 수 있다. 예를 들어 메뉴가 수백 개로 늘면
`localStorage` 5MB 한계가 진짜 문제가 된다. 그때는 제안하고 상의해라.

### 범위 밖과 보류

`PLAN.md` 가 명시적으로 뺀 것들이다. 만들지 말라는 게 아니라, **기획 단계에서
이미 검토하고 미룬 것**이라는 뜻이다. 필요해지면 꺼내 쓰면 된다.

- **범위 밖**: 실제 결제 연동 · 서버 · DB · 로그인 · POS 연동 · 다중 매장 관리
- **보류**: 설정 Export / Import, 영화관 모드(좌석 · 회차 선택. 데이터 구조가
  메뉴 주문과 완전히 다르다), 다국어, 사운드 피드백

한 가지는 기획의 보류 항목이었는데 만들었다. **뒤로 가기 버튼**이다.
보류 사유는 "화면 전환 이력(스택) 관리가 필요하다" 였는데, 없으면 잘못 누른
손님이 갇히는 게 더 큰 문제라 판단했다. 스택은 만들지 않고 고정 선형 매핑
(`js/app.js` 의 `BACK`)만 뒀다. 원래 우려했던 복잡도는 피한 셈이다.

---

## 2. 이 프로젝트가 존재하는 이유

기획의 문제의식은 이렇다. 매장 키오스크를 쓰다 보면 반복해서 겪는 불편이 있다.

- 품절이라 메뉴가 사라져서 있었는지 없었는지 헤맨다
- 글씨가 작아 읽기 힘들다
- 잠깐 고민하는 사이 화면이 초기화된다
- 지금까지 뭘 담았는지 보려면 화면을 옮겨야 한다

그래서 **그 넷을 고친 키오스크**를 만드는 것이 목적이다. 키오스크를 흉내 내는
게 아니라, 개선안을 만져 볼 수 있는 형태로 증명하는 것이 핵심이다.

이 넷을 지우거나 약화시키면 프로젝트가 "그냥 키오스크 클론" 이 된다.
더 나은 방식으로 같은 문제를 푸는 건 환영이지만, 문제 자체를 없애는 건
제품의 목적을 바꾸는 일이다.

여기에 더해, 한 업종에 하드코딩하지 않고 누구나 자기 매장을 구성해 볼 수 있게
관리자 편집 모드를 붙였다. 프리셋은 완제품이 아니라 편집 가능한 초기 데이터다.

---

## 3. 디자인 이력

디자인은 세 번 갈아엎었다. **무엇을 골랐는지보다 왜 바꿨는지가 중요하다.**

| 차수 | 사용한 스킬 | 결과 |
|---|---|---|
| 1차 | `soft-skill` | Editorial Luxury × 비대칭 벤토. 크림 배경, Instrument Serif, 테라코타 |
| 2차 | `frontend-design` | 사이니지 방향. 좌측 레일 + 균일 그리드 + 어두운 결제 바 |
| 3차 | `taste-skill` | 현재. 밝은 리테일. 상단 탭 + 페이지 그리드 + 밝은 결제 바 |

### 1차가 왜 버려졌나

사용자 반응은 "디자인이 너무 이상한 것 같아" 였다.

원인은 스킬 선택이 아니라 **브리프를 잘못 읽은 것**이었다. soft-skill 의
매크로 여백과 비대칭 레이아웃 지침을, 정보 밀도가 생명인 키오스크에 그대로
적용했다. 공교롭게 frontend-design 스킬은 "AI 가 기본값으로 몰리는 세 가지 룩"
중 첫 번째로 **크림 + 하이컨트라스트 세리프 + 테라코타**를 지목하는데,
1차 결과물이 정확히 그것이었다.

### 2차에서 3차로 왜 갔나

사용자가 실제 키오스크 사진을 가져왔다. 매머드커피와 배스킨라빈스는 좌측 레일이
아니라 상단 탭을 쓰고, 훨씬 밝고 가벼우며, 스크롤이 아니라 페이지를 넘긴다.

**레퍼런스가 있으면 레퍼런스가 브리프다.** 2차의 사이니지 방향은 그 자체로
틀리지 않았지만, 사용자가 원하는 것과 달랐다.

### 여기서 남는 것

디자인 스킬을 새로 적용할 때, 스킬의 기본 다이얼은 대체로 랜딩 페이지 기준이다.
이 프로젝트는 랜딩 페이지가 아니다. taste-skill 자신도 "multi-step product UI
에는 쓰지 말라" 고 명시한다.

그래서 3차에서는 시각 언어 · 타이포 · 색 · anti-slop 규칙만 가져오고, 랜딩 전용
규칙(히어로 스택, eyebrow 카운트, 로고월)은 홈 화면에만 적용했다.
다이얼도 기본값 대신 "전 연령대가 서서 쓰는 공공 단말" 이라는 성격에서 다시 뽑았다.

이건 3차 시점의 판단이다. 다른 결론에 도달할 수도 있다.

---

## 4. 현재 디자인 시스템의 선택들

3차에서 정한 것들이다. 일관성을 위해 지금은 이렇게 되어 있다는 기록이지,
넘을 수 없는 선은 아니다. 다만 **하나를 바꾸면 전체가 흔들리는 종류**라
부분적으로 깨는 것보다 통째로 다시 정하는 편이 낫다.

### 모서리

```
타일 · 카드        --r-card
주요 액션 버튼      --r-pill
보조 · 유틸 버튼    --r-btn
입력                --r-input
칩 · 배지           --r-chip
```

taste-skill 의 Shape Lock 요구를 따른 것이다. 근거는 "둥근 버튼과 각진 카드가
섞이면 깨져 보인다". 예외는 결제 리더기 삽화뿐인데, 그건 컴포넌트가 아니라
실물 카드를 그린 그림이라 실제 물건의 모서리를 쓴다.

### 색

- 액센트는 **매장이 고르는 값**이다. 하드코딩하면 테마 기능이 죽는다.
  `var(--accent)` / `var(--accent-deep)` 를 쓴다.
- 액센트 하나만 쓴다. 두 번째 강조색을 넣으면 어느 쪽이 행동 유도인지 흐려진다.
- 순수 흑백은 안 쓴다. 깊이가 죽는다.

액센트 밝기에 따라 CTA 대비가 무너지는 문제가 있어서, `js/app.js` 가 상대 휘도를
계산해 필요할 때만 잉크 버튼으로 바꾼다. 테마를 추가하면 이 계산이 자동으로 처리한다.

### 타이포

Pretendard 한 종류. 가격도 본문과 같은 얼굴이다.

2차에서는 가격 · 열량 · 주문번호에 Archivo Expanded 를 따로 썼다. 안내판 숫자의
성격을 주려는 의도였는데, 3차 레퍼런스가 전부 가격을 본문과 같은 얼굴로 쓰길래
걷어냈다. 폰트 요청이 하나 줄어든 부수 효과도 있었다.

한글은 어절 중간에서 끊기므로 긴 문장에는 `word-break: keep-all` 이 필요하다.

### 모션

의도적으로 거의 없다. 합계 금액의 자리수 롤 하나가 유일한 장식이다.

근거는 두 가지다. 무인으로 오래 도는 화면이라 무한 애니메이션이 실제 비용이고,
전 연령대가 쓰는 공공 단말에서 움직임은 대체로 방해가 된다.
결제 진행 중 리더기 애니메이션 2개는 "기계가 일하는 중" 을 알리는 상태 표현이라
예외로 뒀다. 전부 `prefers-reduced-motion` 에서 멈춘다.

### 홈 화면 레이아웃

사용자가 직접 지정했다. "큰 사진 한 장을 중간 위에 배치, 밑에 시작하기 버튼 및
큰 글씨 모드". 맥도날드 대기 화면 구조다. 이건 판단이 아니라 요구사항이다.

---

## 5. 스킬 사용 이력

이 머신에 설치된 스킬 중 이 프로젝트와 관련된 것들이다.

**사용함**
- `soft-skill` (1차, 폐기)
- `frontend-design` (2차)
- `taste-skill` (3차, 현재)

**설치되어 있으나 미사용**
`taste-skill-v1` · `gpt-tasteskill` · `minimalist-skill` · `brutalist-skill` ·
`redesign-skill` · `stitch-skill` · `image-to-code-skill` · `theme-factory` ·
`brand-guidelines` · `brandkit` · `imagegen-frontend-web` · `imagegen-frontend-mobile`

**이 프로젝트와 무관**
`algorithmic-art` · `canvas-design` · `doc-coauthoring` · `internal-comms` ·
`mcp-builder` · `output-skill` · `skill-creator` · `slack-gif-creator` ·
`web-artifacts-builder` · `webapp-testing` · `remotion-*` (11종)

전체 목록은 `ls ~/.claude/skills/` 로 확인하면 된다. 이 목록은 낡을 수 있다.

---

## 6. 알려진 미완 항목

**기본 메뉴 사진을 추가했다 (2026-09-08).**

GPT 내장 이미지 생성 기능을 한 번 사용해 8열 × 6행 시트를 만들고,
메뉴 사진 46개와 홈 구성 사진 2개로 잘랐다. `assets/images/`의 JPEG를
정적 파일로 제공한다. 카페·버거 홈은 구성 사진을, 분식·편의점 홈은 대표 메뉴
사진을 사용한다. 원본, 생성 프롬프트와 자르기 좌표도 같은 폴더에 보관했다.

기존 저장 데이터를 초기화하지 않도록 사진이 빈 기본 메뉴는 이름으로 기본 사진을
찾는다. 사용자가 지정한 사진이 우선이며, 새 이름의 메뉴는 기존 듀오톤 폴백을
유지한다. 이미지 데이터 자체를 localStorage에 복사하지 않는다.

생성 횟수를 줄이는 대신 해상도는 제한된다. 실제 원본은 1448 × 1086이며,
개별 사진은 약 179px 폭이다. 모바일 메뉴용으로 사용하고, 큰 홈 사진의
선명도를 더 높이는 작업은 추후 별도 생성으로 개선할 수 있다.

---

## 7. 무언가를 바꿀 때 같이 흔들리는 것들

연결 관계 지도다. 금지가 아니라 파급 범위 안내다.

| 바꾸는 것 | 같이 확인할 것 |
|---|---|
| `--touch` 계열 토큰 | 기획서의 60px 하한. 390pt 폭 휴대폰에서 환산값이 그 아래로 내려가는지 |
| 그리드 열 수 | 페이지당 개수 계산 (`js/customer.js`). 큰 글씨 모드 분기도 함께 |
| 스키마 필드 추가 | `normalize()`. 안 넣으면 저장 후 사라진다 |
| 테마 추가 | CTA 대비 자동 보정이 그 밝기를 제대로 처리하는지 |
| 화면 추가 | `BACK` 맵, `WATCHED` 배열(무동작 감시 대상), `onEnter()` |
| 화면 구조 리팩터 | `node .claude/audit.js`. 마크업과 CSS 가 어긋나면 조용히 깨진다 |
| 커밋 이벤트 추가 | 관리자 타이핑 중 재렌더 루프가 생기는지 (`'config-quiet'` 참고) |

## 8. 홈 액션과 언어 선택 (2026-09-08)

사용자 요청으로 주문 시작하기와 큰 글씨 모드를 한 줄에 배치했다.
언어 선택은 한국어·영어·일본어·중국어(간체) 4개로 확정해 홈에 추가했다.
기존 보류 항목 중 다국어를 구현한 것으로, 고객 안내와 기본 프리셋 메뉴·옵션·설명을
번역 사전으로 제공한다. 관리자 입력을 덮어쓰지 않도록 표시 단계에서만 번역한다.
선택 언어는 기기 설정에 저장하고, 언어 선택 버튼 자체는 각 언어의 이름으로 유지한다.

## 9. 세로 홈 사진과 국기 (2026-09-08)

사용자 요청으로 홈 메인 사진의 세로 길이를 늘리고 업종별 홈 전용 사진을 새로 만들었다.
UI UX Pro Max의 터치 영역·버튼 간격 지침을 참고했다. 사진이 남는 높이를 채우고,
안내 문구와 언어 선택, 가로 액션 버튼은 아래에 이어지도록 구성했다.

GPT 내장 이미지 생성 1회로 2×2 시트를 생성해 561×701 JPEG 4개로 잘랐다.
`assets/images/home-v2/`에 원본·프롬프트·자르기 좌표를 함께 보관한다.
이전 메뉴 사진은 유지하며, 기존의 저해상도 홈 기본 사진을 새 이미지로 교체했다.
관리자에서 지정한 홈 사진과 직접 변경한 메뉴 사진이 우선한다.

언어 버튼에는 한국·미국·일본·중국 SVG 국기를 붙였다. 국가 표시는 시각 보조로 쓰고,
언어 이름과 선택 상태를 함께 제공한다. 국기 이미지는 보조 이미지이므로 중복 낭독하지 않는다.

## 10. 장바구니 바 재구성 (2026-09-08)

사용자가 선택한 `#payBar`를 UI UX Pro Max의 비활성 상태 안내 기준으로 수정했다.
상단에 주문 내역 버튼·수량·합계, 하단에 전체 폭 결제 버튼을 배치했다.
장바구니가 비어 있어도 수량 0과 합계 0원을 남기며, 하단에는 메뉴 선택 안내를 표시한다.
CSS로 클릭만 막던 기존 처리 대신 실제 `disabled` 속성으로 빈 상태의 결제를 차단한다.
상품을 담으면 테마색 결제 버튼으로 바뀐다. 기존 장바구니 확인 경로와 금액 롤은 유지한다.
