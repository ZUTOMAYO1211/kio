# 키오스크 웹 시뮬레이터 기획서

## 1. 개요
- **목적**: 실제 매장 키오스크의 UX를 웹으로 재현하고, 불편했던 부분을 개선한 개인 제작물
- **수익화 없음** — 포트폴리오 / 학습용
- **기술 스택**: HTML / CSS / Vanilla JS (빌드 도구·프레임워크 없음)
- **배포**: GitHub Pages (`index.html` 진입점, 정적 호스팅)
- **저장**: `localStorage` (서버 없음)

## 2. 실행 환경
| 환경 | 용도 |
|---|---|
| 휴대폰 (세로) | 실제 키오스크처럼 조작·테스트하는 **메인 환경** |
| PC (가로) | 개발·확인용. 화면 중앙에 세로 9:16 비율 **키오스크 프레임**으로 렌더링 |

- 기준 해상도: 1080 × 1920 비율을 CSS로 스케일링 (가로 화면에서는 좌우 여백 처리)
- 터치 + 마우스 동시 대응, 최소 터치 영역 60px 이상

## 3. 업종 모드
프리셋 + 자유 편집을 **동시에** 지원한다. 프리셋은 "편집 가능한 초기 데이터"일 뿐이며, 불러온 뒤 이름·사진·설명·가격·카테고리를 전부 수정할 수 있다.

- **기본 프리셋**: 카페 / 패스트푸드 / 분식·식당 / 편의점·간식 / 빈 템플릿
- **영화관 모드**: 좌석 선택·회차 선택 등 구조가 완전히 다르므로 **1차 범위에서 제외**, 확장 과제로 분리
- 모든 프리셋은 동일한 데이터 스키마를 사용 → 모드 전환 = 데이터 교체

## 4. 화면 구성

### 4-1. 고객 흐름
| 순서 | 화면 | 내용 |
|---|---|---|
| 1 | **시작(대기) 화면** | 대표 이미지 + 매장 로고/이름 + `주문 시작하기` 버튼. 이미지는 편집 모드에서 교체 가능 |
| 2 | **주문 유형 선택** | 매장 식사 / 포장 (프리셋에 따라 생략 가능) |
| 3 | **메뉴 목록** | 상단 카테고리 탭 + 메뉴 그리드(사진·이름·가격). 하단 고정 장바구니 바 |
| 4 | **옵션 선택 시트** | 수량, 사이즈, 온도(HOT/ICE), 추가 옵션(체크·라디오), 옵션별 추가금 실시간 반영 |
| 5 | **장바구니** | 항목 수량 조절·삭제, 총액, `전체 취소` / `추가 주문` / `결제하기` |
| 6 | **결제 화면** | 결제 수단 선택(카드/간편결제/현금) → 결제 진행 애니메이션 (모의 결제, 실제 연동 없음) |
| 7 | **주문 완료** | 주문번호 발급, 영수증 요약, N초 후 시작 화면 자동 복귀 |

### 4-2. 관리자 흐름
| 화면 | 내용 |
|---|---|
| **진입** | 시작 화면 로고 **5회 연속 탭** (3초 이내) → PIN 입력 → 편집 모드 |
| **매장 설정** | 매장명, 로고, 시작 화면 이미지, 테마 색상, 통화·언어 |
| **카테고리 관리** | 탭 추가/삭제/이름 변경/순서 변경, 표시 여부 토글 |
| **메뉴 관리** | 메뉴 추가·수정·삭제, 사진(업로드 또는 URL), 이름, 설명, 가격, 품절 처리, 옵션 그룹 연결 |
| **옵션 그룹 관리** | 옵션 그룹 생성(단일/다중 선택), 옵션별 추가금 |
| **데이터 관리** | 프리셋 불러오기, 초기화 |
| **주문 로그** | 완료된 모의 주문 내역 확인 |

## 5. 데이터 구조 (요약)
```
config = {
  store:      { name, logo, heroImage, theme, currency },
  categories: [ { id, name, order, visible } ],
  menus:      [ { id, categoryId, name, desc, price, image, soldOut, optionGroupIds } ],
  optionGroups:[ { id, name, type: 'single'|'multi', options: [{ name, price }] } ]
}
```
- 이미지는 Base64 또는 외부 URL 저장 (localStorage 용량 고려해 리사이즈 후 저장)

## 6. 차별점 (적용)
기존 키오스크에서 불편했던 지점을 개선한다. 아래 4가지만 이번 범위에 포함한다.

1. **품절 메뉴 명시** — 숨기지 않고 회색 처리 + '품절' 배지로 표시. 메뉴가 사라져서 헤매는 상황 방지
2. **큰 글씨 모드** — 한 번의 토글로 폰트·버튼 확대 (고령 사용자 대응)
3. **무동작 타임아웃 안내** — "N초 후 초기화됩니다 / 계속하기" 모달로 갑작스러운 초기화 방지
4. **장바구니 상시 노출** — 하단 고정 바로 현재 담긴 수량·금액을 항상 확인

## 7. 보류 (이번 범위 제외, 추후 검토)
당장 만들지 않는다. 필요해지면 이 목록에서 꺼내 쓴다.

- **되돌리기 버튼** — 실수로 넘어간 단계를 한 단계 되돌림. 화면 전환 이력(스택) 관리가 필요해 구조가 잡힌 뒤 검토
- **설정 Export / Import** — 만든 매장 구성을 JSON 파일로 백업·공유
- **영화관 모드** — 좌석 선택·회차 선택. 데이터 구조가 메뉴 주문과 완전히 달라 별도 설계 필요
- **다국어 지원**
- **사운드 피드백** — 터치음, 주문 완료음

## 8. 범위 밖
- 실제 결제 연동, 서버·DB, 로그인, POS 연동, 다중 매장 관리
